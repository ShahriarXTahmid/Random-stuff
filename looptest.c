#include <stdio.h>

int main() 
{
    // Loop to print "I love Bangladesh" 100 times
    for (int i = 1; i <= 100; i++) {
        printf("%d: I love Bangladesh\n", i); // Printing the line number with the message
    }
    return 0; // Exit successfully
}